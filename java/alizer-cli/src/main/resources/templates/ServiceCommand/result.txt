{#for entry in result}
Component:{entry.key} Services:{#if entry.value.isEmpty} No services found{#else}{#for service in entry.value} Service:{service.name} Operators:{#for operator in service.operators}{operator}{#if hasNext},{/if}{/for} {#if hasNext},{/if}{/for}  {/if}{#if hasNext},{/if}
{/for}