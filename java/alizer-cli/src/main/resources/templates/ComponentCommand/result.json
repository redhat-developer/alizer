{ "success":  true,
  "components":
  [
    {#for comp in result}
    {
      "path": "{comp.root}",
      "languages": [
        {#for language in comp.languages}
        "{language.name}"{#if hasNext},{/if}
        {/for}
      ]
    }{#if hasNext},{/if}{/for}
  ]
}